# 取标点 => getTalkPunc 替换过程

> 注意每一行的空格


`\s*结束 如果`

`否则如果 `
`else if (`

`如果 `
`if (`

`否则`
`else`

` 则`
`)`

`寻找文本\(句子,\s*"(.+?)",\s*(-1|0)\)\s*=\s*`
`sent.indexOf("\1") == `

`<>\s?-1`
`> -1`

`取标点\s? =`
`punc =`

`语气导向\s?=`
`tone ==`

` 或 `
` || `

` 且 `
` && `

`取文本长度\(句子\)`
`sent.length()`

`取文本右边\(句子,\s*(\d+)\)\s*=\s*`
`sent.right(\1) == `

`取文本左边\(句子,\s*(\d+)\)\s*=\s*`
`sent.left(\1) == `

`取文本右边\(句子,\s*(\d+)\)` 手动替换（2处）
`sent.right(\1)`

`判断 左(\d)\s+分支 "(.)"`
`if (_left\1 == "\2")`

`分支 (否则|else)`
`else`

`分支 "(.)"`
`else if (_left1 == "\1")`

`\s*结束 判断`


`(punc\s?=\s?".{1,2})"`
`\1;`

`是否为中文\(左(\d)\)`
`isChinese(_left\1)`

`\s?==?\s?真`
` == true`

`\s==?\s?假`
` == false`

`知道系列\(句子\)`
`isKnowFormat(sent)`

`变量` 手动搜索（2处），注意加大括号


`'` 手动搜索删除（1块）


`取数组成员数\(正则匹配\(句子, "(.+?)"\)\)\s>\s0`
`canRegExp(str, "\1") == true`

`句子\s=\s`
`sent == `